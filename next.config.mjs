/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['unsplash.com','images.unsplash.com','cdn.dummyjson.com'],
      },
    swcMinify: true,
};

export default nextConfig;
